import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-training-new',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
